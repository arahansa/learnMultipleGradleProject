subprojects {
    apply plugin: 'war'
    apply plugin: 'eclipse-wtp'
    apply plugin: 'idea'

    repositories {
        mavenCentral()
    }
    dependencies {
        providedCompile 'javax.servlet:javax.servlet-api:3.1.0'
    }
}